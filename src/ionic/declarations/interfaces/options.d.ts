declare module 'cordova-plugin-advanced-background-mode' {

    interface Options {
        title?: string,
        text?: string,
        bigText?: boolean,
        resume?: boolean,
        silent?: boolean,
        hidden?: boolean,
        color?: string,
        icon?: string
    }

}
